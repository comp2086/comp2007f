@{
	string title = ViewBag.Title;
}

<div class="container">
	<div class="col-md-10">
		<div class="col-sm-6 col-md-3">
			<br />
			<h1>@Model.Name</h1>
			<br />
			<img src="@Url.Content(Model.FullImage)" />
			<div class="caption">
				<h3></h3>
				<h4>@string.Format("{0:C}", Model.Price)</h4>
				<p>@Model.FullDesc</p>
			</div>
		</div>
	</div>
</div>

