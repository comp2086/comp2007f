@{
	string title = ViewBag.Title;
	string backLink = (string)ViewData["BackLink"];
}

<div class="container">
	<div class="col-md-10">
		<div class="col-sm-6">
			<br />
			<h1>@Model.Name</h1>
			<br />
			<img src="@Url.Content(Model.FullImage)" />
			<div class="caption">
				<h3></h3>
				<h4>@string.Format("{0:C}", Model.Price)</h4>
				<p>@Model.FullDesc</p>
			</div>
			<h3><a href="@Url.Action("")">Back</a></h3>
		</div>
	</div>
</div>

